import theme from '@/node_modules/@fastybird-com/theme/store/common'

export const state = () => (theme.state)

export const getters = theme.getters

export const actions = theme.actions

export const mutations = theme.mutations
