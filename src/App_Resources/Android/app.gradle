// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "com.fastybird.io.application"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
